; Minimal diagnostic: set border red and loop forever
; No RET, so it does not depend on BASIC call stack state.

org 32768

START:
    ld a, 2         ; Red border
    out (254), a

LOOP:
    jr LOOP

end START
